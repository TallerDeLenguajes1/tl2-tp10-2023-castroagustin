@model tl2_tp10_2023_castroagustin.ViewModels.CrearUsuarioViewModel

<form asp-controller="Usuario" asp-action="CreateUser">
    <div class="form-group">
        <label asp-for="@Model.NombreDeUsuario">Nombre Usuario:</label>
        <input asp-for="@Model.NombreDeUsuario" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="@Model.Contrasenia">Contraseña:</label>
        <input asp-for="@Model.Contrasenia" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="@Model.Rol">Rol:</label>
        <input asp-for="@Model.Rol" class="form-control" />
    </div>

    <button type="submit" class="btn btn-primary">Crear</button>
</form>